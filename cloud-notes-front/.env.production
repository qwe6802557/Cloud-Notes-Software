# 生产环境配置
# 注意：后端路由无 /api 前缀，Nginx 直接代理 /auth/、/notes/ 等路径，故此处留空
REACT_APP_API_URL=
# 静态资源前缀：CRA 会替换 index.html 中的 %PUBLIC_URL%，让 static 走子路径
PUBLIC_URL=/cloudNotes
# 路由 basename：React Router 由此识别子路径，留空表示根路径
REACT_APP_ROUTER_URL=/cloudNotes
