# 工作记录 (Changelog)

## 2026-09-24

### 🚀 性能优化 (Performance)

- **首屏加载极速优化（HTTP/2 + 静态强缓存 + 接口去重）**：
  - **启用 HTTP/2 多路复用**：生产 Nginx 443 端口全面开启 HTTP/2 支持，彻底消除 HTTP/1.1 线头阻塞与单域名并发限制。
  - **静态资源长期强缓存 (Immutable)**：为 `/static/` 目录下带 Hash 的所有 JS/CSS/Media 资源配置一年强缓存（`Cache-Control: public, max-age=31536000, immutable`），消除每次开机访问时的 304 协商往返，实现本地磁盘 0ms 秒开。
  - **入口 HTML 协商缓存保障**：为 `/index.html` 强制配置 `no-cache, no-store, must-revalidate`，确保发版后新版本即刻生效。
  - **首屏接口并发合并与依赖项纠偏**：
    - `NavTree` 中隔离 `selectedNotebook` 的依赖重生成，彻底解决组件初始化触发二次重复请求的问题。
    - 为 `getNotebooks` 注入 In-flight Promise 并发共享机制，将首屏 `/api/notebooks` 耗时从 848ms（并发排队）暴降至 42ms，提速 20 倍。
  - **外部 WebFont 字体异步非阻塞加载（方案3）**：
    - 从 `typography.css` 彻底剥离阻塞式 `@import`，消除 CSSOM 关键渲染路径阻断，使 `main.css` 自包含直出。
    - 在 `index.html` 中引入 Preconnect 预连接与 `media="print" onload="this.media='all'"` 异步样式拉取，利用本地系统优质字体实现首屏毫秒级秒开（FCP 瞬间呈现），外部字体就绪后平滑升级。

## 2026-09-23

### 🎨 新增与优化 (Features & Enhancements)

- **多图片长笔记高性能懒加载与呼吸骨架屏（行业最佳实践方案）**：
  - 针对包含大量高清截图的长笔记，实现纯前端轻量化懒加载（零后端改动）。
  - **视口感知预加载**：基于 `IntersectionObserver` 容器级感知与 400px 视口外缓冲，提前静默拉取，滑到时无感秒开，首屏图片网络并发降低 76%+。
  - **微光呼吸骨架屏**：未加载完成前展示流光骨架占位并预留最小高度，彻底消除累积布局偏移（CLS 闪烁与跳跃）。
  - **平滑渐显与异常容错**：加载完成后 0.3s 平滑淡入显现；网络波动加载失败时提供优雅的异常卡片与「点击重试」机制。
  - **全屏画廊无缝兼容**：画廊预览收集器全面兼容 `data-src`，即便图片尚未滑入正重视口，依然能在大图画廊中正常索引并浏览全图。

- **编辑器长图画廊预览交互优化**：
  - 支持长图等比例平滑缩放，锁定初始比例为缩小底线。
  - 锁定水平 X 轴偏移，彻底杜绝缩放或拖拽时图片左右跑偏与消失的问题。
  - 实现视口级纵向平滑拖拽接管（Vertical Drag-to-Scroll），支持鼠标上下抓手漫游。
  - 滚轮精准分流：鼠标在长图内时仅触发缩放（不滚动容器），鼠标在长图外时触发容器正常滚动。
  - 工具栏新增「1:1 复位」功能，一键恢复比例并平滑滚动回顶部。

- **选中复制与快捷键支持**：
  - 增加划词悬浮复制胶囊功能，选中文字后可快速复制。
  - 顶栏增加一键复制功能。
  - 支持 `Ctrl+C` 快捷键复制时的即时状态反馈。

### 🚀 部署与运维 (Deployment)

- 将上述代码改动（长图优化与复制优化）完整提交至 GitHub 仓库并部署。
