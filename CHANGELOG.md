# 工作记录 (Changelog)

所有关键特性迭代、缺陷修复与架构演进记录。

---

## 2026-09-23

### 🎨 新增与优化 (Features & Enhancements)

#### 1. 全屏图片画廊与超长截图交互深度重构
- **缩放底线锁定与平滑缩放**：
  - 彻底解除此前为防止变形而在 CSS 中对长图行内缩放的压制（移除 `transform: none !important;`）；
  - 配置 `minScale: 1`：严格以初始自适应舒适宽度为缩小底线，长图绝不会缩小至比初始更小；当缩放到 1.0x 时，底栏缩小按钮与复位按钮自动置灰禁用（`operation-disabled`）；
  - 支持等比例平滑放大至最大 `5x`（步进 0.5x），解决长截图中微小字符看不清的问题。
- **水平 X 轴锁定与彻底根除长图消失问题**：
  - 禁用 `rc-image` 对长图的原生二维拖拽（`movable: !isTall`），防止其在 `mouseup` 时误触发 `getFixScaleEleTransPosition` 回弹计算（`dragRebound`），消除长截图被错误弹飞移出屏幕视口、造成黑屏的严重隐患；
  - 在 `imageRender` 中强制重置行内 `translate3d` 的 X 分量为 `0px`，配合 `.ant-image-preview-wrap { overflow-x: hidden !important; }`，实现长图在视口内物理级水平居中锁定，严禁左右跑偏。
- **视口级纵向平滑拖拽接管（Vertical Drag-to-Scroll）**：
  - 在长图预览激活时，通过非被动事件监听代理实现原生抓手拖拽阅读；
  - 鼠标在长图预览区域任意位置按住左键上下拖动，直接平滑驱动视口滚动条（`scrollTop`），松开平稳停驻在当前阅读位置；
  - 鼠标光标状态精细化：悬停呈现 `cursor: grab`，按住拖动呈现 `cursor: grabbing`；
  - 增加 `-webkit-user-drag: none` 与 `user-select: none`，消除浏览器原生拖拽幽灵虚影。
- **滚轮行为精准分流**：
  - 在外层容器上使用非被动事件监听（`{ passive: false }`）代理 `wheel` 事件；
  - **鼠标在长图区域内**：调用 `e.preventDefault()` 阻止外层视口滚动，滚轮仅触发长图的平滑放大与缩小；
  - **鼠标在长图外部（黑色背景空白区域）**：不拦截默认行为，滚轮正常驱动长图视口上下阅读滚动。
- **工具栏与一键 1:1 复位**：
  - 底栏新增 `OneToOneOutlined`（1:1 复位）按钮，在放大状态下高亮激活，在 1.0x 初始状态下自动置灰；
  - 点击复位按钮可瞬间将缩放比重置回 1.0x，并平滑滚动回长截图顶端（`scrollTop: 0`）；
  - 保留并优化在新标签页查看原图等辅助工具。

#### 2. 文本划选悬浮复制胶囊与一键复制系统
- **划选悬浮复制胶囊（Bubble / Pill）**：
  - 在编辑区（CodeMirror）或预览区（ByteMD Viewer）鼠标划词选中任意文字时，智能计算坐标并在选区正上方浮出高质感暗色磨砂玻璃药丸胶囊；
  - 智能视口边界防溢出保护：顶部空间不足时自动翻转至选区正下方；
  - 点击胶囊瞬间触发复制，胶囊微动效切为翠绿「已复制 ✓」反馈后优雅淡出。
- **全通道快捷键监听与富文本增强**：
  - 监听底层 `copy` 事件与 `Ctrl+C` / `⌘C` 键盘快捷键，按键时浮动胶囊同步触发即时反馈动画；
  - 预览区划选复制时，自动将 `text/html` 富文本与 `text/plain` 纯文本双格式注入系统剪贴板，支持向外部富文本编辑器完美粘贴格式。
- **顶栏一键复制全文**：
  - 移除下拉小图标，采用与「新建」按钮完全一致的标准单体按钮交互设计；
  - 点击或悬停呼出下拉菜单，支持一键复制 Markdown 源码与预览富文本排版内容。

### 🚀 部署与运维 (Deployment)
- 经由 `powershell -ExecutionPolicy Bypass -File .\deploy-core.ps1 -Mode front` 全自动编译构建生产包；
- 产物安全分发至腾讯云服务器（1.15.171.111）并平滑热重载 Nginx 服务；
- 针对超长截图（2919px）与文本复制功能，在生产线上环境（[notes.yanggenbwebsite.site](https://notes.yanggenbwebsite.site/)）完成 Chrome DevTools 实机全流程回归验证，全部 PASS。

---

## 2026-09-22

### 🌲 核心功能迭代 (Core Features)
- **目录菜单导航树增强**：
  - 支持笔记目录树节点拖拽移动与同级排序，自动持久化存储树层级状态；
  - 修复偏好设置保存时的多层校验逻辑缺陷。
- **排版系统与多端说明文档体系建立**：
  - 重新拟定并规范化 `README.md`，补全 Web 端、Electron 桌面端与 React Native 移动端的多端架构说明；
  - 规范技术文档命名与格式，统一视觉规范与交互设计说明。
